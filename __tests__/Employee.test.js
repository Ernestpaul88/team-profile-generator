const Employee = require("../lib/Employee");

test("Should be able to create Employee object", () => {});

test("getEmail() should return the supplied email", () => {});

test("getRole() should return 'Employee'", () => {});
